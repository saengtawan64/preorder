# บริบทโปรเจกต์ BSD — สำหรับวางเปิด AI session ใหม่

วางไฟล์นี้เป็นข้อความแรกใน session ใหม่ เพื่อให้เข้าใจระบบทั้งหมดโดยไม่ต้องอธิบายซ้ำ
รายละเอียดเชิงลึกอยู่ใน `README.md`

**อัปเดตล่าสุด: 3 ส.ค. 2569 — ระบบใช้งานจริงอยู่ ทำงานครบทั้งสองทาง**

---

## ระบบนี้คืออะไร

**BSD · Banana Secure Deposit** — ระบบภายในให้พนักงานร้านบันทึก/ค้นหารายการมัดจำสินค้า
(ชื่อ, ชื่อเล่น, เบอร์โทร, สินค้า, ยอดมัดจำ, สถานะการรับของ)

**สำคัญที่สุด: Google Sheet ไม่ใช่แค่ที่เก็บข้อมูล แต่เป็นหน้าจอหลักที่พนักงานใช้บันทึก**
จึงต้องเร็ว สวย ไม่รก — และแก้ในชีตแล้วต้องเด้งขึ้นเว็บทันที

```
พนักงาน ──(รหัสผ่านเดียว)──► เว็บ (Cloudflare Pages)
                                    │
                                    ▼
                               Firestore ◄── ฐานข้อมูลหลัก (source of truth)
                               │       ▲
              sync-worker      │       │  functions/api/sheet-webhook.js
              (cron 5 นาที)     ▼       │  (Apps Script ยิงมาเมื่อพิมพ์ในชีต)
                            Google Sheet ┘
```

| ส่วน | ที่อยู่ |
|---|---|
| เว็บ | https://deposit-tracker-app.pages.dev · Pages project `deposit-tracker-app` |
| ตัวซิงก์ | Cloudflare Worker `deposit-tracker-sync-worker` (cron ทุก 5 นาที) |
| ฐานข้อมูล | Firebase `deposit-tracker-2026` (Firestore + Auth) |
| ชีต | id `1AvMnLCzcIrksa8n8_-c3fneE5qfCqZ2Ztb_b9qcPBUc` — **แท็บเดียวชื่อ `Deposits`** |
| บัญชีพนักงาน | `bnn2207@gmail.com` (รหัสอยู่ใน Firebase Auth เท่านั้น ไม่มีในโค้ด) |
| Service account | `deposit-sync@deposit-tracker-2026.iam.gserviceaccount.com` |
| กุญแจ (ห้าม commit) | `C:/Users/saeng/Downloads/deposit-tracker-2026-afb994b15800.json` |
| โค้ด | GitHub `saengtawan64/preorder` branch `main` |

---

## ผังคอลัมน์ในชีต (แท็บเดียว เก็บทุกสถานะ)

| คอลัมน์ | เก็บอะไร | หมายเหตุ |
|---|---|---|
| A | วันที่และเวลา | เติมอัตโนมัติ · `2/8/2569 11:07` · **บังคับเป็นข้อความล้วน** |
| B | ชื่อจริง | |
| C | ชื่อเล่น | |
| D | เบอร์โทร | |
| E | สินค้าที่มัดจำ | |
| F | ยอดมัดจำ (บาท) | |
| G | สถานะ | dropdown 3 ค่า + เปลี่ยนสีตามสถานะ |
| H | `depositId` | **ซ่อน** · ระบบจัดการเอง ห้ามแก้มือ |
| I | อัปเดตล่าสุด | **ซ่อน** · ระบบจัดการเอง |

สถานะ 3 แบบ: `รอการจัดส่งสินค้า` / `รับสินค้าแล้ว` / `ลบแล้ว`
**ไม่มีการย้ายหรือลบแถวใด ๆ** — "ลบ" คือเปลี่ยนสถานะเป็น `ลบแล้ว` เท่านั้น
เปลี่ยน dropdown ในชีต → เด้งขึ้นเว็บ, กดปุ่มในเว็บ → เด้งลงชีต

---

## ⚠️ กฎเหล็ก 4 ข้อ (เคยพังมาแล้วทุกข้อ — อย่าทำซ้ำ)

**1. ห้ามใช้ `values.append` ของ Sheets API** — ใช้เขียนระบุแถว `A{n}:I{n}` เท่านั้น
append ก๊อปสีหัวตารางลงแถวใหม่, ดัน banding/CF ให้เลื่อน, และยึดคอลัมน์แรกที่
"มองเห็น" → พอซ่อนคอลัมน์ A แถวใหม่เลื่อนไปขวา ทำให้เกิดแถวซ้ำไม่รู้จบ

**2. ห้ามซ่อนคอลัมน์ A** — ซ่อนได้แค่ H และ I (อยู่ท้ายสุด)

**3. ห้าม `setValue()` สตริงหน้าตาเหมือนวันที่ โดยไม่ `setNumberFormat('@')` ก่อน**
Sheets จะแปลงเป็นค่าวันที่ แล้วอ่านกลับได้ `Date` → กลายเป็นข้อความยาวแบบอังกฤษ

**4. ล้างข้อมูล: ลบ Firestore ก่อน แล้วค่อยเคลียร์ชีต** — ไม่งั้น worker sync กลับใน 5 นาที

**เวลาต้องตรงกันทั้งระบบ** — `bangkokTimestamp()` (`src/utils.js`) กับ `formatThai()`
(`appsscript/onEditSync.gs`) ต้องได้สตริงเดียวกันเป๊ะ อย่าใช้ `toLocaleString`

---

## หลักการที่ต้องรักษาไว้

- **เบราว์เซอร์ห้ามมี credential ที่คุยกับ Google Sheets ได้** — sync ทุกทิศทางผ่าน
  ฝั่งเซิร์ฟเวอร์เท่านั้น (Worker / Pages Function / Apps Script) ถ้าฟีเจอร์ใหม่
  ทำให้ต้องเปิดให้เบราว์เซอร์แตะชีตตรง ๆ → **หยุดแล้วถามก่อน** นั่นคือรูรั่วเดิมที่ปิดไปแล้ว
- **เบราว์เซอร์แก้ข้อมูลธุรกิจใน Firestore เองไม่ได้** — `firestore.rules` ยอมให้แค่
  soft-delete กับเปลี่ยนสถานะ การแก้ไขวิ่งผ่าน `functions/api/update-deposit.js`
  ที่ตรวจ ID token ก่อน
- **Google Sheet ต้องเป็น "จำกัด" (Restricted)** ห้าม publish สาธารณะ
- **รหัสผ่านห้ามอยู่ในโค้ด** — ตั้งใน Firebase Auth เท่านั้น
- **บัญชีเดียวใช้ร่วมทั้งร้าน** — ตกลงกันแล้วว่ายอมรับ trade-off (ไม่รู้ว่าใครทำอะไร)
  แลกกับความง่าย ถ้าจะเปลี่ยนเป็นบัญชีแยกคนต้องถามก่อน

---

## แบรนด์ + ธีม

โลโก้ **BSD** โล่ทอง + แม่กุญแจ + กล้วย (`public/logo.svg`, วาดเป็น SVG เอง)
ธีม **ทอง `#F5C518` บนดำ `#141416`**

**ทองใช้เฉพาะแบรนด์** (โลโก้ / เมนูที่เลือก / ปุ่มหลัก / ยอดเงิน)
**ห้ามใช้ทองเป็นสีสถานะ** — สถานะเป็น เหลือง/เขียว/แดง ให้ตรงกับสีในชีต

หน้าเว็บเป็นไซด์บาร์เข้ม + 4 เมนู: รอรับของ / รับแล้ว / ลบแล้ว / สรุปยอด
บนจอมือถือตารางจะกลายเป็นการ์ดเรียงลง (ไม่ต้องเลื่อนแนวนอน)

---

## การ deploy (ผู้ใช้เป็นคนรัน)

```bash
# เว็บ
npm run build
npx wrangler pages deploy dist --project-name=deposit-tracker-app

# ตัวซิงก์
cd sync-worker && npx wrangler deploy
npx wrangler deployments list        # เช็คว่าเข้าจริง — ดูวันที่ล่าสุด
```

**Apps Script วางมือเท่านั้น** — เปิดชีต → ส่วนขยาย → Apps Script → วาง
`appsscript/onEditSync.gs` ทั้งไฟล์ → ตั้ง Script Properties → ตั้ง trigger
installable "On edit" ชี้ที่ `onEditInstallable` → รัน `setupSheet` หนึ่งครั้ง

> เคยเจอ: ผู้ใช้บอกว่า deploy แล้ว แต่จริง ๆ ไป deploy Pages ไม่ใช่ Worker
> อาการ: แท็บ archive เก่าถูกสร้างกลับมาเอง + มีแถวผังเก่าโผล่
> วิธีเช็ค: `npx wrangler deployments list` ดูวันที่ล่าสุด

---

## สำรองข้อมูล

`backup.mjs` (หรือดับเบิลคลิก `backup.bat`) → zip ลง
`C:\Users\saeng\Downloads\DepositTracker-Backups\` เก็บ 8 ไฟล์ล่าสุด
มี Windows Scheduled Task ชื่อ **"BSD Backup"** รันทุกวันอาทิตย์ 20:00

ในไฟล์สำรองมี: ซอร์สโค้ด, `git-history.bundle` (clone กลับได้), ข้อมูล Firestore +
ชีต, และ `RESTORE.md` — **ไม่เก็บกุญแจ/รหัส/secrets โดยตั้งใจ**

---

## สิ่งที่ AI ทำเองไม่ได้ (ต้องให้ผู้ใช้ทำ)

1. วาง Apps Script ในชีต (ต้องผ่านหน้าจอ Google)
2. ตั้ง/อ่าน secrets ของ Cloudflare กับรหัสผ่าน Firebase Auth
3. ทดสอบ flow ที่ต้องล็อกอิน (AI ไม่มีรหัสพนักงาน)

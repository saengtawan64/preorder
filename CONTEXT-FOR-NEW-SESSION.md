# บริบทโปรเจกต์ BSD — สำหรับวางเปิด AI session ใหม่

วางไฟล์นี้เป็นข้อความแรกใน session ใหม่ เพื่อให้เข้าใจระบบทั้งหมดโดยไม่ต้องอธิบายซ้ำ
รายละเอียดเชิงลึกอยู่ใน `README.md`

**อัปเดตล่าสุด: 3 ส.ค. 2569 — ระบบใช้งานจริงอยู่ ทำงานครบทั้งสองทาง**

---

## ระบบนี้คืออะไร

**BSD · ระบบหลังร้านบานาน่า** — เดิมเป็นระบบมัดจำอย่างเดียว ตอนนี้ขยายเป็นหลังร้าน
ไซด์บาร์แบ่ง 2 หมวด:

- **มัดจำ** — รอรับของ / รับแล้ว / ลบแล้ว / สรุปยอด (ชื่อ, ชื่อเล่น, เบอร์โทร,
  สินค้า, ยอดมัดจำ, สถานะการรับของ)
- **ยอดขาย** — แดชบอร์ดวิเคราะห์ยอดขายมือถือ (**ยอดขายอ่านอย่างเดียว** แต่ **เป้าแก้ได้**
  เก็บใน Firestore ใช้ร่วมทั้งร้าน) + เครื่องคำนวณค่างวดผ่อน

## การเข้าใช้งาน — รหัส 5 หลัก (ตรวจฝั่งเซิร์ฟเวอร์)

หน้าจอเป็นแป้นตัวเลขเหมือนปลดล็อกมือถือ แต่ **รหัสไม่เคยอยู่ในโค้ดฝั่งเบราว์เซอร์**

1. กดรหัส → POST `/api/pin-login`
2. เซิร์ฟเวอร์เทียบกับ `settings/pins` ใน Firestore
3. ถูก → คืน **Firebase custom token** → เบราว์เซอร์แลกเป็น session จริง
4. `firestore.rules` จึงยังเห็น `request.auth` เป็นผู้ใช้จริงเหมือนเดิม

- **รหัสตั้งต้นมาจาก secret `STAFF_PINS`** (seed ครั้งแรกลง Firestore) — **ห้ามใส่รหัสในโค้ด
  เด็ดขาด repo นี้เป็น public**
- **เพิ่ม/ตั้งชื่อ/ลบรหัสได้จากเมนู "จัดการรหัส" ในเว็บ** (ไม่ต้อง deploy) สูงสุด 10 รหัส
  - เบราว์เซอร์เห็นแค่ `{index, label, hint}` เช่น `3•••7` — **ไม่เคยได้รับรหัสจริง**
    จึงส่งทั้งรายการกลับไปไม่ได้ API เลยรับเป็น**คำสั่งทีละอย่าง** `add`/`remove`/`rename`
  - ลบต้องแนบ `hint` ที่หน้าจอเห็น (กันลบผิดตัวเมื่อลำดับเลื่อน) · **เหลือ 1 รหัสลบไม่ได้**
  - `settings/pins` = `entries:[{pin,label,addedAtIso}]` · ของเก่าเป็น `pins:[...]`
    → `_lib/pins-store.js` อ่านได้ทั้งคู่ และเขียนครั้งแรกจะล้าง `pins` เก่าทิ้ง
- กันเดารหัส: ผิด 8 ครั้ง ล็อก 15 นาที นับแยกตาม IP (เก็บเป็นค่าแฮช ไม่เก็บ IP จริง)
- uid ที่ใช้คือ `shop-staff` — `verify-firebase-token.js` ตรวจแค่ลายเซ็น/โปรเจกต์/วันหมดอายุ
  ไม่ผูกกับบัญชีใดบัญชีหนึ่ง ระบบเดิมจึงทำงานต่อได้ทั้งหมด

---

## มัดจำค้างนาน + ติดตามลูกค้า (2026-08-04)

- `src/aging.js` — อายุมัดจำจาก `createdAtIso` เป็น**จำนวนวันที่ผ่านไป** (ไม่ใช่วันปฏิทิน
  โซนเวลาจึงไม่มีผล) · 30 วัน = เหลือง · 60 วัน = แดง · นับเฉพาะ "รอรับของ"
- การ์ด "ค้างเกิน 30 วัน" **เทาเมื่อไม่มีรายการค้าง** — การ์ดที่แดงตลอดจะถูกมองข้าม
- เมนู "รอรับของ" เรียง**เก่าสุดก่อน** เมนูอื่นยังใหม่สุดก่อน (เป็นรายการที่ต้องลงมือทำ)
- ปุ่มแจ้งลูกค้า: **คัดลอกข้อความก่อน สำเร็จแล้วค่อยบันทึกว่าติดตาม** — คัดลอกพลาด
  = ไม่บันทึก เพราะยังไม่ได้แจ้งจริง (บันทึกว่าแจ้งแล้วทั้งที่ยังไม่แจ้ง อันตรายกว่า)
- `followedUpAtIso`/`followUpCount` **เป็นฟิลด์เว็บล้วน ไม่ขึ้นชีต** — ผังคอลัมน์ชีตเปราะบาง
  และ updateMask ของทั้ง `upsertDepositFromSheet`/`updateDepositFromWeb` ไม่มี 2 ฟิลด์นี้
  จึงไม่ทับกัน
- `/api/follow-up` แยกจาก `/api/update-deposit` **โดยตั้งใจ** — ไม่รับข้อมูลธุรกิจเลย
  จึงแก้ชื่อ/ยอดเงินไม่ได้แม้โดยบังเอิญ

---

**สำคัญที่สุด: Google Sheet ไม่ใช่แค่ที่เก็บข้อมูล แต่เป็นหน้าจอหลักที่พนักงานใช้บันทึก**
จึงต้องเร็ว สวย ไม่รก — และแก้ในชีตแล้วต้องเด้งขึ้นเว็บทันที

```
พนักงาน ──(รหัสผ่านเดียว)──► เว็บ (Cloudflare Pages)
                                    │
                                    ▼
                               Firestore ◄── ฐานข้อมูลหลัก (source of truth)
                               │       ▲
              sync-worker      │       │  functions/api/sheet-webhook.js
              (cron 5 นาที)     ▼       │  (Apps Script ยิงมาเมื่อพิมพ์ในชีต)
                            Google Sheet ┘
```

| ส่วน | ที่อยู่ |
|---|---|
| เว็บ | https://deposit-tracker-app.pages.dev · Pages project `deposit-tracker-app` |
| ตัวซิงก์ | Cloudflare Worker `deposit-tracker-sync-worker` (cron ทุก 5 นาที) |
| ฐานข้อมูล | Firebase `deposit-tracker-2026` (Firestore + Auth) |
| ชีตมัดจำ | id `1AvMnLCzcIrksa8n8_-c3fneE5qfCqZ2Ztb_b9qcPBUc` — **แท็บเดียวชื่อ `Deposits`** · **ส่วนตัว** |
| ชีตยอดขาย | id `1tjMRZSljGLy1irwUukDdV5bNcwSFTq5JoPCHSh0zoBs` gid `614260076` · **เปิดสาธารณะ + publish to web** (เจ้าของงานเลือกเอง) |
| บัญชีพนักงาน | `bnn2207@gmail.com` (รหัสอยู่ใน Firebase Auth เท่านั้น ไม่มีในโค้ด) |
| Service account | `deposit-sync@deposit-tracker-2026.iam.gserviceaccount.com` |
| กุญแจ (ห้าม commit) | `C:/Users/saeng/Downloads/deposit-tracker-2026-afb994b15800.json` |
| โค้ด | GitHub `saengtawan64/preorder` branch `main` |

---

## ผังคอลัมน์ในชีต (แท็บเดียว เก็บทุกสถานะ)

| คอลัมน์ | เก็บอะไร | หมายเหตุ |
|---|---|---|
| A | วันที่และเวลา | เติมอัตโนมัติ · `2/8/2569 11:07` · **บังคับเป็นข้อความล้วน** |
| B | ชื่อจริง | |
| C | ชื่อเล่น | |
| D | เบอร์โทร | |
| E | สินค้าที่มัดจำ | |
| F | ยอดมัดจำ (บาท) | |
| G | สถานะ | dropdown 3 ค่า + เปลี่ยนสีตามสถานะ |
| H | `depositId` | **ซ่อน** · ระบบจัดการเอง ห้ามแก้มือ |
| I | อัปเดตล่าสุด | **ซ่อน** · ระบบจัดการเอง |

สถานะ 3 แบบ: `รอการจัดส่งสินค้า` / `รับสินค้าแล้ว` / `ลบแล้ว`
**ไม่มีการย้ายหรือลบแถวใด ๆ** — "ลบ" คือเปลี่ยนสถานะเป็น `ลบแล้ว` เท่านั้น
เปลี่ยน dropdown ในชีต → เด้งขึ้นเว็บ, กดปุ่มในเว็บ → เด้งลงชีต

---

---

## แดชบอร์ดยอดขาย (`src/sales.js` + `src/sales-view.js`)

อ่านชีตยอดขายตรงจากเบราว์เซอร์ด้วย **URL แบบ publish**
(`/spreadsheets/d/e/2PACX-.../pub?gid=614260076&single=true&output=csv`)
ไม่ใช่ `/export` — เพราะ publish แยกจากการแชร์ลิงก์ ถ้าภายหลังจำกัดสิทธิ์ชีต
แดชบอร์ดจะยังทำงานอยู่

**โครงชีต:** แถว 3 = หัวตาราง · คอลัมน์ 0 = มาร์กเกอร์เดือน (`เดือน มกราคม 2569`
เว้นวรรคไม่สม่ำเสมอ) · คอลัมน์ 1 = วันที่ หรือ `รวม` · **คอลัมน์ 2–17 = 8 แบรนด์
มือถือ แบรนด์ละ 2 ช่อง `[ยอด, จำนวน]`** (OPPO 2, VIVO 4, SS 6, REALME 8,
TECNO 10, Honor 12, XIAOMI 14, IPHONE 16) · คอลัมน์ 18+ ยังไม่ได้ใช้

**รวมยอดจากแถวรายวันเอง ไม่เชื่อแถว "รวม" ของชีต** (ตรวจข้ามแล้ว 64 ค่า ตรงหมด)
**ไม่ใช้ Chart.js** — CSP เป็น `script-src 'self'` กราฟแท่งเป็น CSS ล้วน

> ⚠️ **อย่าสลับหลักการระหว่างสองชีต** — ชีตมัดจำมีข้อมูลลูกค้า ต้องส่วนตัวและ
> แตะผ่านเซิร์ฟเวอร์เท่านั้น · ชีตยอดขายเป็นตัวเลขรวม เปิดอ่านจากเบราว์เซอร์ได้

---

## ⚠️ กฎเหล็ก 5 ข้อ (เคยพังมาแล้วทุกข้อ — อย่าทำซ้ำ)

**1. ห้ามใช้ `values.append` ของ Sheets API** — ใช้เขียนระบุแถว `A{n}:I{n}` เท่านั้น
append ก๊อปสีหัวตารางลงแถวใหม่, ดัน banding/CF ให้เลื่อน, และยึดคอลัมน์แรกที่
"มองเห็น" → พอซ่อนคอลัมน์ A แถวใหม่เลื่อนไปขวา ทำให้เกิดแถวซ้ำไม่รู้จบ

**2. ห้ามซ่อนคอลัมน์ A** — ซ่อนได้แค่ H และ I (อยู่ท้ายสุด)

**3. ห้าม `setValue()` สตริงหน้าตาเหมือนวันที่ โดยไม่ `setNumberFormat('@')` ก่อน**
Sheets จะแปลงเป็นค่าวันที่ แล้วอ่านกลับได้ `Date` → กลายเป็นข้อความยาวแบบอังกฤษ

**4. ล้างข้อมูล: ลบ Firestore ก่อน แล้วค่อยเคลียร์ชีต** — ไม่งั้น worker sync กลับใน 5 นาที

**5. แก้ CSP แล้วต้องทดสอบจาก origin ที่ deploy จริง + hard reload**
Google ตอบ CSV ด้วย **307 redirect ไป `doc-XX-XX-sheets.googleusercontent.com`
(โฮสต์เปลี่ยนทุกครั้ง)** ต้องมี `https://*.googleusercontent.com` ใน `connect-src`
สองจุดที่หลอก: CSP รายงาน violation เป็น URL **ก่อน** redirect (`docs.google.com`
ที่อนุญาตอยู่แล้ว) และ CSP ที่บังคับใช้มากับตัวเอกสาร — หน้าในแคชจะยังถือกฎเก่า

**เวลาต้องตรงกันทั้งระบบ** — `bangkokTimestamp()` (`src/utils.js`) กับ `formatThai()`
(`appsscript/onEditSync.gs`) ต้องได้สตริงเดียวกันเป๊ะ อย่าใช้ `toLocaleString`

---

## หลักการที่ต้องรักษาไว้

- **เบราว์เซอร์ห้ามมี credential ที่คุยกับ Google Sheets ได้** — sync ทุกทิศทางผ่าน
  ฝั่งเซิร์ฟเวอร์เท่านั้น (Worker / Pages Function / Apps Script) ถ้าฟีเจอร์ใหม่
  ทำให้ต้องเปิดให้เบราว์เซอร์แตะชีตตรง ๆ → **หยุดแล้วถามก่อน** นั่นคือรูรั่วเดิมที่ปิดไปแล้ว
- **เบราว์เซอร์แก้ข้อมูลธุรกิจใน Firestore เองไม่ได้** — `firestore.rules` ยอมให้แค่
  soft-delete กับเปลี่ยนสถานะ การแก้ไขวิ่งผ่าน `functions/api/update-deposit.js`
  ที่ตรวจ ID token ก่อน
- **Google Sheet ต้องเป็น "จำกัด" (Restricted)** ห้าม publish สาธารณะ
- **รหัสผ่านห้ามอยู่ในโค้ด** — ตั้งใน Firebase Auth เท่านั้น
- **บัญชีเดียวใช้ร่วมทั้งร้าน** — ตกลงกันแล้วว่ายอมรับ trade-off (ไม่รู้ว่าใครทำอะไร)
  แลกกับความง่าย ถ้าจะเปลี่ยนเป็นบัญชีแยกคนต้องถามก่อน

---

## แบรนด์ + ธีม

โลโก้ **BSD** โล่ทอง + แม่กุญแจ + กล้วย (`public/logo.svg`, วาดเป็น SVG เอง)

ธีม **"สิ่งพิมพ์" (editorial)** — กระดาษครีม `#F5F1E6` + หมึกดำ `#1B1A16`
ทั้งเว็บพูดภาษาเดียวกับหน้าล็อกอิน: ไม่มีเงา ไม่มีมุมมน คั่นด้วย**เส้นบาง**อย่างเดียว

**ฟอนต์มี 3 หน้าที่ ห้ามสลับกัน**
- `--serif` (Georgia) → หัวข้อเท่านั้น
- `--mono` (IBM Plex Mono) → **ตัวเลขทุกตัว** + ป้ายกำกับเล็ก + หัวคอลัมน์
- `--font` (Prompt) → เนื้อความและปุ่ม (ภาษาไทยอ่านง่ายสุด)

**หมึก (`--rule`) คือสีของการโต้ตอบ** — เมนูที่เลือก / ปุ่มหลัก / ปุ่มที่ถูกกด
**ทองใช้เฉพาะ "เงิน" กับกราฟ** — เป็นแบรนด์ ไม่ใช่สถานะ
**ห้ามใช้ทองเป็นสีสถานะ** — สถานะเป็น เหลือง/เขียว/แดง ให้ตรงกับสีในชีต

หน้าล็อกอินและไซด์บาร์**ไม่เปลี่ยนตามโหมดกลางวัน/กลางคืน** (เป็นเปลือกของแอป)
โหมดมืดคือ "ฉบับกลางคืน" — เทาอมน้ำตาลทั้งชุด ห้ามเป็นเทาอมฟ้า

เมนู 4 อัน: รอรับของ / รับแล้ว / ลบแล้ว / สรุปยอด + ยอดขาย / คำนวณผ่อน
บนจอมือถือตารางจะกลายเป็นการ์ดเรียงลง (ไม่ต้องเลื่อนแนวนอน)

---

## การ deploy (ผู้ใช้เป็นคนรัน)

```bash
# เว็บ
npm run build
npx wrangler pages deploy dist --project-name=deposit-tracker-app

# ตัวซิงก์
cd sync-worker && npx wrangler deploy
npx wrangler deployments list        # เช็คว่าเข้าจริง — ดูวันที่ล่าสุด
```

**Apps Script วางมือเท่านั้น** — เปิดชีต → ส่วนขยาย → Apps Script → วาง
`appsscript/onEditSync.gs` ทั้งไฟล์ → ตั้ง Script Properties → ตั้ง trigger
installable "On edit" ชี้ที่ `onEditInstallable` → รัน `setupSheet` หนึ่งครั้ง

> เคยเจอ: ผู้ใช้บอกว่า deploy แล้ว แต่จริง ๆ ไป deploy Pages ไม่ใช่ Worker
> อาการ: แท็บ archive เก่าถูกสร้างกลับมาเอง + มีแถวผังเก่าโผล่
> วิธีเช็ค: `npx wrangler deployments list` ดูวันที่ล่าสุด

---

## สำรองข้อมูล — ปิดไว้แล้ว

เจ้าของงานเลือก **ไม่สำรองอะไรทั้งสิ้น** งานตั้งเวลา "BSD Backup" ถูกลบแล้ว

สคริปต์ `backup.mjs` / `backup.bat` ยังอยู่ในโฟลเดอร์ (gitignored) ถ้าจะกลับมาใช้
ให้รันมือ — แต่ต้องมีไฟล์กุญแจ service account ก่อน ซึ่งตอนนี้**หายไปจากเครื่อง**
(ระบบจริงไม่กระทบ เพราะ Cloudflare มีสำเนาของตัวเองใน secrets)

---

## สิ่งที่ AI ทำเองไม่ได้ (ต้องให้ผู้ใช้ทำ)

1. วาง Apps Script ในชีต (ต้องผ่านหน้าจอ Google)
2. ตั้ง/อ่าน secrets ของ Cloudflare กับรหัสผ่าน Firebase Auth
3. ทดสอบ flow ที่ต้องล็อกอิน (AI ไม่มีรหัสพนักงาน)

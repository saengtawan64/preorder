{
  "name": "deposit-tracker-sync-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cron worker that mirrors Firestore deposits into a private Google Sheet.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
